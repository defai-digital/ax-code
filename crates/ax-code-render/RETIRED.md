# RETIRED — `ax-code-render`

Rust N-API OpenTUI render core (ADR-046). Removed from the Cargo workspace
members list and no longer packaged.

Successor for “native” is **`ax-code-tui`** (standalone Ratatui UI), not this
FFI overlay. Keep this tree only as historical reference until a follow-up
deletes it entirely.
