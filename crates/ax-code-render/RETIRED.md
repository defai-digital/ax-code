# RETIRED — `ax-code-render`

Rust N-API OpenTUI render core (ADR-046). Removed from the Cargo workspace
members list and no longer packaged.

There is no successor renderer mode. Zig/OpenTUI is the sole TUI path. Keep
this tree only as historical reference until a follow-up deletes it entirely.
