{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@ax-code/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "main": "./dist/main.js",
  "scripts": {
    "dev": "bun ./src/main.ts --dev",
    "dev:dry-run": "bun ./src/main.ts --dry-run --dev",
    "build:main": "bun ./src/packaging/build.ts",
    "build": "bun ./src/packaging/build.ts && bun ./src/main.ts --dry-run",
    "package:mac": "bun ./src/packaging/mac.ts",
    "smoke:renderer": "pnpm --dir ../app run build && bun ./src/packaging/build.ts && bun ./src/packaging/renderer-smoke.ts",
    "smoke:packaged": "bun ./src/packaging/smoke.ts",
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@ax-code/sdk": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/node22": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "electron": "catalog:",
    "typescript": "catalog:"
  }
}
