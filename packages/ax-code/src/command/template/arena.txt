Run a multi-provider arena (best-of-N). Use the arena tool.

Task: $ARGUMENTS

Instructions:
1. Ensure modes.arena.enabled is true (config); if the tool reports disabled, tell the user how to enable it.
2. Gather brief context (key files, constraints) if helpful.
3. For approach comparison only: call arena with mode=plan (default).
4. For real multi-model implementation: call arena with mode=implement (worktree-isolated writers + verify-first ranking). Requires git.
5. Present the ranked results and recommend a winner. Do not auto-merge implement worktrees.
6. Never run concurrent writers on the main workspace.
