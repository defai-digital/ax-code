Reproduce the issue in $ARGUMENTS, apply the smallest fix, then re-run the same check before reporting done.

This command starts the builtin issue-to-verified-fix workflow. Do not skip the before-signal.
