Run one focused shell command in ${directory}. Use workdir instead of cd; it applies only to this call. Use the repository root for Git pathspecs.

Actual shell: `${shell}`. Use its syntax. In Bash/sh on Windows, discard output with /dev/null, never nul/NUL (unrestorable files). Unquoted redirects and & belong to the outer shell even after cmd /c. Keep stderr visible.

Use Bash for git, builds, tests, package managers, process control, and aggregate queries. Prefer dedicated file tools for discovery, reading, searching, and editing.

Minimize local-model round trips:
- Prefer single-pass commands over loops or repeated scans.
- Count tracked/source files, excluding dependency/generated trees (.git, node_modules, .venv, venv, dist, build, target).
- Keep factual queries under 500 characters; never assume `/testbed` or enumerate dozens of extensions.
- Once evidence answers the request, synthesize instead of repeating queries.
- Batch independent checks when output stays clear.

Run tests directly, never through head/tail or progress-hiding filters. Use the tool timeout, not nested timeout commands. For slow tests, servers, or watchers set run_in_background: true and follow the SAME run with bash_output; kill_shell stops it. Output beyond ${maxLines} lines or ${maxBytes} bytes is saved. Read captured output for summaries; NEVER rerun a suite merely to get counts or change filters. Missing summaries do not prove success. Report incomplete/timeout results honestly. If a pipeline is necessary, preserve the check's exit status using this shell's syntax; a filter's status is not the test result.

Do not commit, install packages, change global state, or run destructive commands unless requested.
