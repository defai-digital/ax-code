Submit the structured goal contract. Use this only as the Goal Plan Writer when converting an objective into acceptance criteria, a verification plan, non-goals, and (for code-change) a non-binding implementation approach plus task checklist. New code-change submissions also require assurance: version 1, sourcePaths, source references with roles, and executable checks covering every AC id. Each check specifies its id, acceptanceIds, exact command, purpose and declared environment. Commands run through verify_project goalCheck and must assert their target identity and acceptance outcomes. Do not invent source authority or successful observations. Do not call this tool to mark a goal complete. The rendered plan must stay under 8192 bytes: keep every field concise (short single-line items); oversized submissions are rejected and must be shortened and resubmitted.
