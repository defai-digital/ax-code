Submit the structured goal contract. Use this only as the Goal Plan Writer when converting an objective into acceptance criteria, a verification plan, non-goals, and (for code-change) a non-binding implementation approach plus task checklist. Do not call this tool to mark a goal complete. The rendered plan must stay under 8192 bytes: keep every field concise (short single-line items); oversized submissions are rejected and must be shortened and resubmitted.
