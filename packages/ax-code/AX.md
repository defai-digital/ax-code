<!--
Generated by: ax-code
Last updated: 2026-03-26
Complexity: enterprise (334 files, ~67337 LOC)
Refresh: Run `/init` or `ax-code init --refresh` to update
-->

# ax-code
**Type:** cli | **Language:** TypeScript | **PM:** bun | **Version:** 1.3.2

**Stack:** SolidJS, Hono, Zod, Drizzle, Effect, Vercel AI SDK, ESM, TypeScript

## Build & Development
```bash
bun install    # Install dependencies
bun build   # Build project
bun test    # Run tests
bun lint    # Lint code
bun dev     # Development mode
bun typecheck  # Type check
```

**Additional scripts:**
- `bun random` - echo 'Random script updated at $(date)' && echo 'Change queued successfully' && echo 'Another change made' && echo 'Yet another change' && echo 'One more change' && echo 'Final change' && echo 'Another final change' && echo 'Yet another final change'
- `bun clean` - echo 'Cleaning up...' && rm -rf node_modules dist
- `bun format` - echo 'Formatting code...' && bun run --prettier --write src/**/*.ts
- `bun docs` - echo 'Generating documentation...' && find src -name '*.ts' -exec echo 'Processing: {}' \;
- `bun deploy` - echo 'Deploying application...' && bun run build && echo 'Deployment completed successfully'

## Architecture
### Key Directories
- `src/` - Source code
- `test/` - Tests

**Entry point:** `./bin/ax-code`

### Key Files
- `package.json` - Package manifest
- `tsconfig.json` - TypeScript config
- `Dockerfile` - Docker containerization
- `bunfig.toml` - Bun config

## Project-Specific Rules
- ESM imports require `.js` extension: `import { x } from './y.js'`
- Use ES module syntax (`import`/`export`), not CommonJS (`require`)
- Use `zod` for runtime validation
- Use `bun` as the package manager (not npm)

## Code Patterns
### TypeScript
```typescript
import { something } from "./module.js"
```
```typescript
import { z } from "zod"
const schema = z.object({ name: z.string() })
```

## Development Tips
- ESM imports require .js extension even for .ts source files

## Troubleshooting
- **Module not found?** Check `.js` extension in import path