{
  "name": "@ax-code/daemon",
  "version": "0.1.0",
  "description": "Rust-powered background daemon for persistent file tree and real-time indexing",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "ax-code-daemon",
    "triples": {
      "defaults": false,
      "additional": [
        "aarch64-apple-darwin",
        "x86_64-apple-darwin",
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu"
      ]
    }
  },
  "files": [
    "index.js",
    "index.d.ts"
  ],
  "scripts": {
    "build": "napi build --manifest-path ../../crates/ax-code-daemon/Cargo.toml --platform --release --js index.js --dts index.d.ts",
    "build:debug": "napi build --manifest-path ../../crates/ax-code-daemon/Cargo.toml --platform --js index.js --dts index.d.ts"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  },
  "license": "MIT"
}
