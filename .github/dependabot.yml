# Keep GitHub Actions up to date automatically.
#
# Keep pinned action SHAs current, including action runtime migrations.

version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
    commit-message:
      prefix: "ci"
