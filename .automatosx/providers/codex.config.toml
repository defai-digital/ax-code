# Copy this snippet into ~/.codex/config.toml for Codex CLI global MCP registration.
[mcp_servers.automatosx]
command = "ax"
args = ["mcp", "serve"]
env = { AUTOMATOSX_BASE_PATH = "/Users/akiralam/code/ax-code" }
startup_timeout_sec = 30
tool_timeout_sec = 60
