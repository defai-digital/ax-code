# Nine-step review: session-messages-parts

## Step 1 Establish Scope and Public Surface

The `session-messages-parts` unit has a deliberately thin public barrel: `packages/ax-code/src/session/message-v2.ts:1` re-exports the namespace implemented in `packages/ax-code/src/session/message-v2-impl.ts:24`. That implementation owns message and part schemas, event contracts, model-message conversion, persistence reads, compaction filtering, and provider-error normalization. The separate batching utility exposes its contract at `packages/ax-code/src/session/part-write-batcher.ts:10` and its constructor at line 21. The audit inventory identifies the same three candidate files and persistence/hot-path risk at `docs/module-quality-audit/modules/session-messages-parts/MODULE-AUDIT.md:5-10`.

## Step 2 Inspect Validation and Trust Boundaries

Persisted identifiers are reconstructed through the common part base at `packages/ax-code/src/session/message-v2-impl.ts:88-92`, while user/assistant records are discriminated by role at lines 352-458. Unsafe integer offsets and retry counts are rejected through `SafeInteger` at lines 26, 73-78, and 141-147. Opaque pagination input is base64url-decoded, strictly parsed, and schema-validated inside a single error boundary at lines 512-533. Provider failures may retain response headers, bodies, and metadata in the public error shape at lines 48-57, so downstream event and storage consumers must continue treating those fields as sensitive diagnostic data; this module itself performs no environment-secret lookup or process execution.

## Step 3 Trace Persistence and Pagination Correctness

Database rows are schema-checked before use; malformed messages and parts are logged with compact issue summaries and omitted at `packages/ax-code/src/session/message-v2-impl.ts:543-575`. Hydration fetches all parts for a page in one query and groups them by message at lines 583-613. Backward pagination uses `(time_created, id)` as a stable descending key, requests one sentinel row, reverses each result page to chronological order, and emits a cursor from the raw tail at lines 923-963. The point lookup binds both message and session IDs at lines 990-1009, preventing a caller from retrieving a known message through the wrong session. The integration regression verifies three non-overlapping pages at `packages/ax-code/test/session/messages-pagination.test.ts:39-60` and cross-session rejection at lines 100-109.

## Step 4 Verify Model Conversion Semantics

Conversion omits empty messages and keys optional caching by provider/model/API/media mode at `packages/ax-code/src/session/message-v2-impl.ts:678-697`. User file parts either become placeholders under `stripMedia` or retain their URL and MIME type at lines 758-779. Completed tools preserve calls/results while separating unsupported media into a following user message at lines 826-907; pending and running calls receive synthetic interrupted results at lines 867-877, avoiding dangling tool calls. Assistant failures are excluded unless an aborted response contains useful non-reasoning content at lines 801-809. These branches are exercised by the attachment regression at `packages/ax-code/test/session/message-v2.test.ts:323-493`, the error filtering cases at lines 706-778, and interrupted-tool assertions at lines 835-923.

## Step 5 Review Hot-path Cost and Ordering

The conversion cache is a `WeakMap`, refuses transient pending/running tool states, and limits each message to three model keys at `packages/ax-code/src/session/message-v2-impl.ts:616-654`. Persistence hydration avoids an N+1 query at lines 583-601. The part batcher coalesces snapshots by part ID, snapshots and clears pending work before serial writes, and chains every flush at `packages/ax-code/src/session/part-write-batcher.ts:48-60`. `forceImmediate` deletes a stale queued copy and joins the same promise chain at lines 75-84. Its consumer schedules streaming deltas at `packages/ax-code/src/session/processor-impl.ts:1075-1083`, forces final text at lines 1086-1107, and drains the queue before the final transaction at lines 1250-1257. Concurrency ordering is specifically tested at `packages/ax-code/test/session/part-write-batcher.test.ts:70-109` and lines 112-145.

## Step 6 Evaluate Ownership and Contracts

Schema ownership, persistence projection, provider conversion, and bus payloads remain co-located in the `MessageV2` namespace. Event contracts use the validated `Info` and `Part` schemas at `packages/ax-code/src/session/message-v2-impl.ts:460-498`, while the barrel at `packages/ax-code/src/session/message-v2.ts:1` shields callers from the implementation filename. The batching primitive stays generic and database-agnostic through an injected `write` function at `packages/ax-code/src/session/part-write-batcher.ts:21-26`; session-specific persistence is supplied only by the processor at `packages/ax-code/src/session/processor-impl.ts:177-182`. This separation keeps coalescing reusable without moving session schemas or transaction policy into the utility.

## Step 7 Classify Failure Handling and Maintenance Risk

Cursor corruption is intentionally converted to a stable `Invalid cursor` exception at `packages/ax-code/src/session/message-v2-impl.ts:523-532`. Provider parser failures fall back to a human-readable unknown error while retaining the original cause at lines 1218-1234, and transient network codes become retryable API errors at lines 1062-1095 and 1168-1180. Batcher writes report the part ID through `onError` before rejecting at `packages/ax-code/src/session/part-write-batcher.ts:39-45`; the production caller installs an observable warning hook at `packages/ax-code/src/session/processor-impl.ts:179-182`. The only type suppression in the principal implementation is the documented SDK `ToolSet` mismatch at `packages/ax-code/src/session/message-v2-impl.ts:912-920`; scans of the three candidate files found no TODO or FIXME markers.

## Step 8 Reconcile Findings and Coverage

The existing register contains no accepted findings at `docs/module-quality-audit/modules/session-messages-parts/MODULE-AUDIT.md:81-85`, and the module directory contains no finding files to disposition or independently reverify. This review found no Critical defect, so no `reverify.md` is warranted. Focused regressions cover schema/cursor handling at `packages/ax-code/test/session/message-v2.test.ts:111-160`, cache stability at lines 1173-1255, pagination, and batch ordering. A non-blocking coverage opportunity remains for direct unit cases around the newest-to-oldest compaction boundary and fallback implemented at `packages/ax-code/src/session/message-v2-impl.ts:1026-1059`; the focused test set has no direct `filterCompacted` case.

## Step 9 Run Verification and Determine Exit

The exact-file Vitest run selected `packages/ax-code/test/session/message-v2.test.ts`, `packages/ax-code/test/session/messages-pagination.test.ts`, and `packages/ax-code/test/session/part-write-batcher.test.ts`; all 3 files and all 42 tests passed. `pnpm --dir packages/ax-code run typecheck` also completed successfully. The exercised behavior includes cursor validation at `packages/ax-code/test/session/message-v2.test.ts:146-160`, same-ID coalescing at `packages/ax-code/test/session/part-write-batcher.test.ts:5-27`, and newest-first streaming at `packages/ax-code/test/session/messages-pagination.test.ts:67-79`. With no Critical finding present and both focused runtime checks and static typing green, the primary reviewer evidence is complete.
