# Nine-step review: server-routes-mcp

## Step 1 Establish Scope and Endpoint Inventory

The `server-routes-mcp` unit exports the lazy `McpRoutes` Hono router at `packages/ax-code/src/server/routes/mcp.ts:36-37`, and the main application mounts it beneath `/mcp` at `packages/ax-code/src/server/server.ts:322`. The router exposes ten operations: status and add at `packages/ax-code/src/server/routes/mcp.ts:44-95`, resource list/read at lines 96-148, four OAuth actions at lines 149-260, and connect/disconnect at lines 261-304. The barrel at `packages/ax-code/src/mcp/index.ts:1` confirms that every handler delegates to the `MCP` namespace rather than owning transport or credential state itself.

## Step 2 Examine Request Trust and Validation Boundaries

The router middleware lets `GET` and `OPTIONS` continue but requires the process-local runtime token for every other method at `packages/ax-code/src/server/routes/mcp.ts:38-43`; the token is 32 random bytes and compared with length checking plus `timingSafeEqual` at `packages/ax-code/src/server/runtime-auth.ts:5-26`. Before this router runs, the server applies optional whole-runtime auth, Basic Auth, privileged-browser origin checks, rate limiting, and CORS at `packages/ax-code/src/server/server.ts:166-207`, then validates and installs the project directory at lines 258-277. MCP names are restricted to 1–64 alphanumeric, underscore, or hyphen characters and resource URIs to 1–8192 characters at `packages/ax-code/src/server/routes/mcp.ts:12-22`. Add requests also pass through the discriminated local/remote configuration schema at lines 83-89; that schema requires a usable local command or HTTP(S) remote URL and is strict about extra fields at `packages/ax-code/src/config/schema-impl.ts:44-94`. The unauthenticated route-local GET exception includes resource contents, but listener startup is constrained to loopback at `packages/ax-code/src/server/server.ts:345-366` and `packages/ax-code/src/runtime/listen-security.ts:27-31`; this is a local trust choice rather than an exposed network bind.

## Step 3 Trace Handler Results and State Transitions

The status handler returns the configured-server status map directly, while add validates its body, calls `MCP.add`, and returns the resulting map at `packages/ax-code/src/server/routes/mcp.ts:61-63` and `:83-94`. The service includes configured entries even when disconnected at `packages/ax-code/src/mcp/impl.ts:818-830`. Resource listing fans into connected clients, and resource reads return `undefined` for a missing/disconnected client or failed read at `packages/ax-code/src/mcp/impl.ts:1180-1205` and `:1236-1262`; the route converts that absence to a typed 404 at `packages/ax-code/src/server/routes/mcp.ts:137-147`. OAuth start and interactive authenticate first reject servers that do not support OAuth at lines 172-176 and 230-234, while callback validates a JSON authorization code before `finishAuth` at lines 198-208. Connect and disconnect delegate their state changes before returning the documented boolean at lines 277-303.

## Step 4 Check Concurrency, Latency, and Resource Bounds

Router construction is memoized by `lazy`, whose cached-return and reset behavior is implemented at `packages/ax-code/src/util/lazy.ts:7-30`; request handlers themselves add no mutable global state. Resource enumeration runs clients concurrently with `Promise.all` and ignores non-connected clients at `packages/ax-code/src/mcp/impl.ts:1180-1204`. Add, connect, and disconnect serialize per server name through `KeyedSerialQueue` at `packages/ax-code/src/mcp/impl.ts:837-856` and `:934-946`, preventing same-name client replacement races without blocking independent servers. Transport creation uses configured timeouts, for example the local connect and initial tool listing at lines 744-808. The long-lived authenticate request is bounded by the callback module's five-minute timeout and 100-entry pending-flow cap at `packages/ax-code/src/mcp/oauth-callback.ts:75-102`, with waiter eviction and timeout cleanup at lines 205-232.

## Step 5 Evaluate Layering and Contract Ownership

HTTP concerns remain concentrated in `packages/ax-code/src/server/routes/mcp.ts`: OpenAPI metadata, request validators, status selection, and JSON serialization. The generic validator converts Zod rejection into the shared 400 envelope at `packages/ax-code/src/server/validation.ts:4-13`, while `withRouteParam` reads the already-validated named parameter at `packages/ax-code/src/server/routes/route-params.ts:44-49`. MCP owns connection lifecycle, trust, SSRF pinning, OAuth flow state, and resource access; representative boundaries are the pinned fetch at `packages/ax-code/src/mcp/impl.ts:79-84`, trust enforcement at `:875-884`, and OAuth public-URL assertion at `:1284-1293`. Generated API evidence agrees on the route paths and validation constraints, including the name and URI parameters at `packages/sdk/openapi.json:32015-32080` and the boolean connect/disconnect responses at `:32374-32459`.

## Step 6 Inspect Failure Semantics and Maintenance Hygiene

Validation failures use `invalidRequest`, missing resource contents use `notFound`, and unsupported OAuth uses a specific 400 message at `packages/ax-code/src/server/routes/mcp.ts:28-34`, `:135-146`, and `:171-176`. Shared envelopes consistently carry name, message, and numeric status at `packages/ax-code/src/server/error.ts:260-288`, while uncaught plain errors are sanitized to an internal-server response at lines 217-239. Two non-Critical contract inconsistencies remain visible: `MCP.connect` logs and returns when configuration is absent at `packages/ax-code/src/mcp/impl.ts:859-873`, so the route still reports `true` at `packages/ax-code/src/server/routes/mcp.ts:278-280`; and `finishAuth` throws for a missing pending transport before its local catch at `packages/ax-code/src/mcp/impl.ts:1462-1470`, producing a generic 500 although the callback route advertises only 400/404 errors at `packages/ax-code/src/server/routes/mcp.ts:187-208`. The candidate route has no catch blocks, TODO/FIXME markers, or suppression directives, and its small `withMcpName` helper is used by every name-bearing handler.

## Step 7 Compare Executable Coverage with the Surface

Direct HTTP tests reject unsafe MCP names, empty local commands, and invalid remote URLs at `packages/ax-code/test/server/route-validation.test.ts:836-920`. The same suite proves mutation-token enforcement and intentional read-only status access at lines 923-950, then covers resource listing, successful read argument forwarding, payload serialization, and missing-resource 404 behavior at lines 953-1023. Those cases exercise the shared `Server.Default()` mount rather than calling handlers in isolation. Route-level gaps remain for add success, invalid name/query parameters on every name-bearing route, all OAuth endpoints, and the success/failure semantics of connect and disconnect; the numerous `packages/ax-code/test/mcp/` tests exercise domain behavior but do not replace those HTTP-contract assertions.

## Step 8 Reconcile the Audit Register and Findings

The existing register says `_none accepted_` at `docs/module-quality-audit/modules/server-routes-mcp/MODULE-AUDIT.md:60-64`, and there were no files under this unit's `findings/` path during review. The false-success connect behavior and missing-flow callback mapping from Step 6 are Low API-contract candidates: both can mislead a client, but neither bypasses the runtime mutation token, leaks credentials, corrupts persisted configuration, or defeats the MCP trust and SSRF controls. They are documented here because the requested output is limited to the protocol artifacts. No Critical evidence exists, so this primary `codex-sol` pass does not create `protocol/reverify.md`.

## Step 9 Run Verification and Close the Review

From `packages/ax-code`, `AX_TEST_FILES=test/server/route-validation.test.ts pnpm exec vitest run` completed with one file and all 43 tests passing. From the repository root, `pnpm --dir packages/ax-code run typecheck` invoked the `tsgo --noEmit` script declared at `packages/ax-code/package.json:8-10` and completed successfully. These results verify the current route, shared validation/auth envelopes, resource behavior, and TypeScript contracts without changing production code. With the source, related service boundaries, generated OpenAPI, tests, and audit ledger examined, all nine review steps for `server-routes-mcp` are complete.
