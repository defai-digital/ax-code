## Step 1 Map and Public Surfaces

The `crate-terminal` unit is a Rust library and N-API addon: `crates/ax-code-terminal/Cargo.toml:8-19` declares both `cdylib` and `rlib` outputs plus the N-API/Serde dependencies, and `crates/ax-code-terminal/build.rs:1-5` performs N-API build setup. The domain surface in `crates/ax-code-terminal/src/lib.rs:10-111` includes lifecycle, viewport, input-event, and mouse types; ANSI styling and cell-buffer types appear at lines 283-432. Four JavaScript-facing JSON adapters are exported at `crates/ax-code-terminal/src/lib.rs:587-615`. Packaging remains registered at `packages/ax-code-terminal-native/package.json:5-24` and `script/build-native.ts:38-43`. No runtime import of `@ax-code/terminal` was found, although `packages/ax-code/package.json:87-99` retains it as a workspace dependency.

## Step 2 Threat and Failure Model

The principal trust transition is caller-controlled text/JSON crossing N-API at `crates/ax-code-terminal/src/lib.rs:587-615`. Malformed JSON is returned as a `napi::Error` at lines 609-615, while viewport dimensions are constrained to 1..=1000 at lines 7-8 and 29-35. Parser failures can corrupt input interpretation or, if an index lands off a UTF-8 boundary, panic the host process; that exact historical risk is recorded as Critical in `docs/module-quality-audit/modules/crate-terminal/findings/AUDIT-crate-terminal-001.md:5-13`. The crate has no filesystem, network, subprocess, credential, thread, timer, or persistent-state access, so review concentrated on panic safety, allocation bounds, malformed escape streams, and FFI error translation.

## Step 3 Correctness and Invariants

Input decoding checks bracketed paste, SGR mouse, CSI keys, then SS3 before scalar fallback at `crates/ax-code-terminal/src/lib.rs:113-180`. The repaired SS3 branch consumes two ASCII prefix bytes plus `ch.len_utf8()` at lines 143-160, preserving a UTF-8 character boundary for both recognized and unknown finals. State-transition errors are explicit at lines 45-77; deserialized cell vectors are normalized before writes at lines 448-509; buffer diffs use checked `get` with a blank fallback at lines 550-585. N-API serialization failures are translated instead of unwrapped at lines 587-615. The width implementation deliberately drops combining marks and uses a fixed wide-codepoint table at lines 512-547; those are observable policy limitations, but this pass did not establish a new accepted defect without an active caller contract.

## Step 4 Performance and Resource Bounds

`parse_input` and `parse_ansi` are forward scans over input (`crates/ax-code-terminal/src/lib.rs:113-180` and `:298-347`), and `diff_buffers` visits each cell in the new viewport at lines 550-585. `CellBuffer::blank` allocates `cols * rows` cloned cells at lines 434-445; the 1000-by-1000 clamp at lines 7-8 bounds that path to one million cells. The JSON adapters at lines 587-615 add serialization copies, and plain text parsing creates one `String` per scalar at lines 163-177. No benchmark was run because repository search found no runtime consumer of `@ax-code/terminal`; consequently no performance finding was accepted without a representative workload or before/after measurements.

## Step 5 Design and Ownership

Pure parsing, width, buffer, and lifecycle logic is testable without N-API, while the thin boundary wrappers are grouped at `crates/ax-code-terminal/src/lib.rs:587-615`; `crates/ax-code-terminal/build.rs:1-5` contains only build integration. The single 937-line file is large but remains organized by terminal-domain concepts, so size alone does not prove a design fault. Ownership is currently dormant/legacy: the central native adapter names fs, diff, index, and parser but not terminal at `packages/ax-code/src/native/addon.ts:1-30` and `:67-86`, while `packages/ax-code/src/cli/cmd/doctor.ts:502-514` says the old Rust-renderer flags are retired in favor of Zig/OpenTUI. Retention is nevertheless explicit in the package manifest and native build list, so removal requires a separate ownership decision.

## Step 6 Dead Code and Hygiene

Static inspection found no `unsafe`, `TODO`, `FIXME`, `HACK`, panic macro, or production `unwrap()` in `crates/ax-code-terminal/build.rs:1-5` or `crates/ax-code-terminal/src/lib.rs:1-617`. The only fallible integer conversion uses `unwrap_or(u16::MAX)` before viewport clamping at `crates/ax-code-terminal/src/lib.rs:599-605`. Runtime call-site search found manifest/build registration but no TypeScript import; this aligns with retirement enforcement at `packages/ax-code/src/cli/cmd/tui/thread.ts:682-691`. I therefore recorded the crate as intentionally retained but presently uncalled, rather than declaring its public functions unreachable solely from a text search.

## Step 7 Test Coverage

The Rust module contains 24 behavioral tests at `crates/ax-code-terminal/src/lib.rs:618-936`, covering paste, keys/mouse, ANSI state, wide cells, diffs, lifecycle, viewport clamping, malformed CSI, buffer repair, SS3, and supplementary CJK widths. The Critical regression has focused cases for F1-F4 at lines 859-865 and unknown ASCII/multibyte finals at lines 883-920. `cargo test --manifest-path crates/Cargo.toml -p ax-code-terminal` passed 24/24 unit tests and 0 doc tests. The two paths listed in `docs/module-quality-audit/modules/crate-terminal/MODULE-AUDIT.md:32-34` test TypeScript TUI cleanup and suspension, as confirmed by `packages/ax-code/test/cli/tui/terminal-cleanup.test.ts:29-54` and `packages/ax-code/test/cli/tui/terminal-suspend.test.ts:4-170`; they do not exercise this N-API crate. Direct wrapper-schema tests remain absent, but the Critical panic invariant is covered below the wrapper that calls `parse_input` at `crates/ax-code-terminal/src/lib.rs:587-590`.

## Step 8 Finding and Fix Disposition

The sole recorded item, `AUDIT-crate-terminal-001`, is Critical/stability and marked `verified-fixed` at `docs/module-quality-audit/modules/crate-terminal/findings/AUDIT-crate-terminal-001.md:5-14`. Its minimal repair is present at `crates/ax-code-terminal/src/lib.rs:143-160`: consume an unknown SS3 final without emitting phantom input and advance by its actual UTF-8 byte width. Regression evidence at lines 883-920 covers repeated unknown sequences and both two-byte and three-byte finals followed by valid text. No source edit or migration is required in this run; rollback would reintroduce a reachable native panic risk. The current assignment records codex-sol as reviewer and ax-code-glm as independent verifier.

## Step 9 Verification and Exit

I re-read `crates/ax-code-terminal/src/lib.rs:143-160` and `:883-920` independently for the Critical item. `cargo test --manifest-path crates/Cargo.toml -p ax-code-terminal` exited 0 with 24 passed, and `cargo clippy --manifest-path crates/Cargo.toml -p ax-code-terminal --all-targets -- -D warnings` exited 0. The second-pass record is in `protocol/reverify.md`. `docs/module-quality-audit/modules/crate-terminal/MODULE-AUDIT.md:11-16` still contains the earlier REVIEWING role order and pending marker; it was read but intentionally not edited because this task limits writes to the requested protocol artifacts. With the existing finding fixed, its regression passing, and the Critical evidence rechecked, this `crate-terminal` reviewer run completes all nine protocol steps.
