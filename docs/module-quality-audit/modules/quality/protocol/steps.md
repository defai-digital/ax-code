# Nine-step review protocol: quality

## Step 1 Scope and entry points

The reviewed `quality` slice covers calibration, critic gating, digest helpers, and the DRE graph renderer named in the request. The unit inventory fixes the root at `packages/ax-code/src/quality` and records the baseline at `docs/module-quality-audit/modules/quality/MODULE-AUDIT.md:5-17`. The externally exercised DRE path is visible in `packages/ax-code/src/server/routes/dre-graph.ts:10-22`, while `packages/ax-code/src/server/routes/dre-graph.ts:140-157` composes verdict, summary, changes, risk, validation, activity, branch, live-refresh, and execution-summary output. Calibration and critic entry points begin at `packages/ax-code/src/quality/calibration-model.ts:264-420` and `packages/ax-code/src/quality/critic.ts:70-75`.

## Step 2 Threat and failure boundaries

Session text crosses an HTML boundary, so the shared encoder in `packages/ax-code/src/quality/dre-graph-format.ts:21-38` escapes markup and makes JSON safe inside scripts; live configuration uses that JSON helper at `packages/ax-code/src/quality/dre-graph-assets.ts:39-47`. Both session page endpoints call `requireCurrentProjectSession` before loading data at `packages/ax-code/src/server/routes/dre-graph.ts:177-209`, and responses disable client caching at `packages/ax-code/src/server/routes/dre-graph.ts:74-76`. The critic is opt-in at `packages/ax-code/src/quality/critic.ts:70-73`; provider, model, timeout, and network failures are contained by the catch/finally path at `packages/ax-code/src/quality/critic.ts:91-137`. That containment is deliberately fail-open—line 134 returns no findings—so the critic must not be treated as a mandatory security gate.

## Step 3 Correctness and edge cases

Calibration bins are confidence-sorted, Laplace-smoothed, and forced nondecreasing at `packages/ax-code/src/quality/calibration-model.ts:108-150`; prediction chooses the nearest bin with a stable lower-start tie break at `packages/ax-code/src/quality/calibration-model.ts:187-212`. Ranking is deterministic by confidence and artifact ID at `packages/ax-code/src/quality/calibration-model.ts:215-245`, and benchmark splitting keeps at least one chronological session on each side at `packages/ax-code/src/quality/calibration-model.ts:339-356`. One low-severity follow-up remains: `laplaceAlpha` accepts any finite number at `packages/ax-code/src/quality/calibration-model.ts:274-276`, while the schema at line 39 does not require positivity; negative input can produce invalid smoothing. Timeline parsing has explicit malformed-input fallback and clamps parsed tool durations at `packages/ax-code/src/quality/dre-graph-timeline.ts:72-100`.

## Step 4 Performance and lifecycle behavior

The session route loads graph, DRE, risk, branch rank, and rollback data concurrently with `Promise.all` at `packages/ax-code/src/server/routes/dre-graph.ts:47-61`, and the index bounds session work to 50 entries at `packages/ax-code/src/server/routes/dre-graph.ts:65-71`. Activity rendering sorts steps once and retains only the top three at `packages/ax-code/src/quality/dre-graph-activity-section.ts:17-24`; tool timing and tool-usage displays are also capped at eight and ten entries at lines 33-60 and 135-148. Browser refreshes are coalesced, detail events use a ten-second gap, and listeners, intervals, and EventSource are cleaned up at `packages/ax-code/src/quality/dre-graph-assets.ts:84-147`. The execution summary serializes overlapping refreshes with a queued rerender at `packages/ax-code/src/quality/dre-graph-assets.ts:197-224`.

## Step 5 Design and ownership

The HTTP layer owns validation, project scoping, data acquisition, and page ordering (`packages/ax-code/src/server/routes/dre-graph.ts:34-95`), while each `dre-graph-*-section.ts` function remains a pure string renderer. Shared presentation policy is centralized in `packages/ax-code/src/quality/dre-graph-format.ts:17-88` and `packages/ax-code/src/quality/dre-graph-widgets.ts:3-119`; the page-wide stylesheet is isolated behind `style()` at `packages/ax-code/src/quality/dre-graph-style.ts:1-2`. The fingerprint layer intentionally reduces live state to scalar/count summaries at `packages/ax-code/src/quality/dre-graph-fingerprint.ts:17-95`. Digest ownership is similarly narrow, although `sha256JsonHex` hashes ordinary `JSON.stringify` order at `packages/ax-code/src/quality/digest.ts:7-12`, so callers must not assume canonical hashes for differently ordered but semantically equal objects.

## Step 6 Maintainability and dormant surfaces

No TODO, FIXME, HACK, or syntactically empty catch was found in the requested source slice. Error handling that appears visually empty is purposeful browser polling suppression at `packages/ax-code/src/quality/dre-graph-assets.ts:133-147`, while server-side branch and rollback degradation logs the error at `packages/ax-code/src/server/routes/dre-graph.ts:53-60`. Two widget exports, `flow` and `stepSummary` at `packages/ax-code/src/quality/dre-graph-widgets.ts:16-71`, have no production references in the repository search and are exercised only by `packages/ax-code/test/quality/dre-graph-widgets.test.ts:16-34`; they are candidates for removal or reconnection. The 537-line CSS string remains cohesive but is the largest maintenance concentration, with responsive rules at `packages/ax-code/src/quality/dre-graph-style.ts:523-534`.

## Step 7 Test coverage assessment

Focused tests cover time-ordered calibration and option normalization (`packages/ax-code/test/quality/calibration-model.test.ts:96-170`), critic budget precedence and non-finite budgets (`packages/ax-code/test/quality/critic-classify.test.ts:22-106`), script-safe live configuration (`packages/ax-code/test/quality/dre-graph-assets.test.ts:14-55`), full fingerprint shape (`packages/ax-code/test/quality/dre-graph-fingerprint.test.ts:27-151`), malformed timeline entries (`packages/ax-code/test/quality/dre-graph-timeline.test.ts:6-56`), and HTML escaping in section renderers such as `packages/ax-code/test/quality/dre-graph-risk-section.test.ts:52-104`. The main gaps are a provider/abort integration test for `Critic.review`, a negative-`laplaceAlpha` test, and a route-level test that proves current-project rejection around the assembled DRE page.

## Step 8 Findings review

There is no `docs/module-quality-audit/modules/quality/findings/` directory or registered finding file. The current ledger likewise says `_none accepted_` at `docs/module-quality-audit/modules/quality/MODULE-AUDIT.md:108-112`. This pass records two non-Critical follow-ups rather than inventing ledger entries outside the requested artifacts: validate positive `laplaceAlpha`, and reconsider the dormant `flow`/`stepSummary` exports. A smaller presentation quirk is already locked into the test: verdict commands are escaped before being passed to `stat` at `packages/ax-code/src/quality/dre-graph-verdict-section.ts:18-24`, then escaped again by `packages/ax-code/src/quality/dre-graph-widgets.ts:7-13`, producing the double-encoded expectation at `packages/ax-code/test/quality/dre-graph-verdict-section.test.ts:66-86`. No Critical evidence exists, so no `reverify.md` is required.

## Step 9 Verification and exit

The exact-file Vitest run covered 19 quality files and passed 68 tests; the suite selection includes every requested DRE renderer test plus calibration and critic classification. `pnpm --dir packages/ax-code run typecheck` also completed successfully. These results corroborate the escaping, empty-state, ranking, budget, timing, and fingerprint behaviors cited above, but they do not erase the explicit gaps in Step 7. The nine review steps for slug `quality` are complete under reviewer `codex-sol`; the requested verifier identity remains `ax-code-glm` for any later independent gate.
