# Nine-step review: server-routes-app-context-schema

## Step 1 Establish Scope and Public Surface

The `server-routes-app-context-schema` unit is the 51-line schema boundary at `packages/ax-code/src/server/routes/app-context-schema.ts:1-51`. It exports response schemas for memory, templates, and the aggregate context payload at lines 10-43, the template request schema at lines 45-47, and three inferred TypeScript contracts at lines 49-51. The file-local `AppContextFile` and `AppContextCheck` shapes at lines 3-8 and 27-33 are exposed only through `AppContextInfo`. This matches the candidate scope and seven-export inventory recorded in `docs/module-quality-audit/modules/server-routes-app-context-schema/MODULE-AUDIT.md:5-17`.

## Step 2 Inspect Input Validation and Trust Boundaries

The only request-controlled field defined here is `key`, constrained by `AppContextTemplateRequest` to the five literal values declared by `AppContextTemplate.shape.key` at `packages/ax-code/src/server/routes/app-context-schema.ts:18-25` and reused at lines 45-47. The POST route installs that schema through `validator("json", ...)` before reading `c.req.valid("json").key` at `packages/ax-code/src/server/routes/app-context.ts:101-106`; failed parses become the shared invalid-request response at `packages/ax-code/src/server/validation.ts:4-12`. Path, command, hash, and timestamp strings in the other schemas describe server output rather than accepted command or filesystem input, so this module adds no process-execution or secret-ingress surface.

## Step 3 Trace Template Contract Correctness

The template key union and response discriminator cannot drift independently because `AppContextTemplateRequest` reuses `AppContextTemplate.shape.key`, and `AppContextTemplateKey` is inferred from that request at `packages/ax-code/src/server/routes/app-context-schema.ts:45-51`. The producer emits the four root templates at `packages/ax-code/src/server/routes/app-context-templates.ts:8-38` and conditionally inserts `dir-rules` only for a nested directory at lines 40-48; all five keys are covered by `templateBody` at lines 53-116. The route locates the validated key, avoids overwriting an existing file, and returns `exists: true` at `packages/ax-code/src/server/routes/app-context.ts:103-120`. The creation behavior is exercised at `packages/ax-code/test/server/app-context-routes.test.ts:98-117`.

## Step 4 Compare Runtime Responses with the Declared Schema

`AppContextInfo` requires directory, worktree, file arrays, templates, checks, and nullable memory at `packages/ax-code/src/server/routes/app-context-schema.ts:35-43`, matching the object assembled by the GET handler at `packages/ax-code/src/server/routes/app-context.ts:73-81`. One low-severity contract drift remains: `getMetadata` declares and returns a `stale` boolean at `packages/ax-code/src/memory/injector.ts:204-229`, while `AppContextMemory` stops at `sections` at `packages/ax-code/src/server/routes/app-context-schema.ts:10-16`. Because the route returns the metadata object directly at `packages/ax-code/src/server/routes/app-context.ts:71-80`, live JSON may contain `stale`, but the generated OpenAPI memory object omits it at `packages/sdk/openapi.json:33570-33596` and the generated SDK type does the same at `packages/sdk/js/src/v2/gen/types.gen.ts:13822-13828`. This is not Critical: required fields remain correct and the discrepancy is an extra undocumented field.

## Step 5 Review Cost, Bounds, and Ordering

All Zod objects and inferred unions are created once at module initialization in `packages/ax-code/src/server/routes/app-context-schema.ts:3-51`; there are no transforms, recursive schemas, or refinements that add request-time work. The corresponding GET route parallelizes its two fixed file probes and template existence checks at `packages/ax-code/src/server/routes/app-context.ts:44-70`. Check discovery is explicitly bounded to four results by `addCheck` at `packages/ax-code/src/server/routes/app-context-checks.ts:81-99`, with early returns from producer loops such as lines 147-152. Array validation remains linear in payload size, and the runtime producers keep those arrays naturally small.

## Step 6 Evaluate Ownership and Type Coupling

The schema file owns the HTTP-facing vocabulary, while filesystem discovery and template rendering stay in neighboring producer modules. Those producers depend on inferred contracts rather than duplicating interfaces: `AppContextTemplateData` and `AppContextTemplateKey` are imported at `packages/ax-code/src/server/routes/app-context-templates.ts:2`, and `AppContextCheckData` is imported at `packages/ax-code/src/server/routes/app-context-checks.ts:5`. Response documentation is attached where routes are defined through `resolver(AppContextInfo)`, `resolver(AppContextTemplate)`, and `resolver(AppContextMemory)` at `packages/ax-code/src/server/routes/app-context.ts:31`, 95, and 134. The `stale` mismatch identified in Step 4 shows the remaining weak link: the metadata producer's return type is handwritten rather than derived from the exported response schema.

## Step 7 Examine Failure Handling and Maintenance Hygiene

The candidate contains declarative schemas only and has no catch blocks, suppression directives, TODOs, mutable state, or side effects across `packages/ax-code/src/server/routes/app-context-schema.ts:1-51`; the static inventory likewise reports zero empty catches and zero TODOs at `docs/module-quality-audit/modules/server-routes-app-context-schema/MODULE-AUDIT.md:22-26`. In the nearest producer, optional package and Makefile reads suppress only `ENOENT` and rethrow every other error at `packages/ax-code/src/server/routes/app-context-checks.ts:117-129`, which is confirmed by unreadable-file regressions at `packages/ax-code/test/server/app-context-routes.test.ts:68-96`. The memory metadata path logs load failures before degrading to null at `packages/ax-code/src/memory/injector.ts:212-216`, consistent with the nullable memory field.

## Step 8 Reconcile Findings and Test Coverage

The existing register says `_none accepted_` at `docs/module-quality-audit/modules/server-routes-app-context-schema/MODULE-AUDIT.md:66-70`, and there are no files beneath this unit's `findings/` path. The focused test checks the GET payload's file, template, and command projections at `packages/ax-code/test/server/app-context-routes.test.ts:12-40` and successful template creation at lines 98-117. It does not directly assert rejection of an invalid template key or compare the memory payload against `AppContextMemory`; those are useful follow-ups, especially for the non-Critical undocumented `stale` field from Step 4. No Critical item exists, so a Critical-only `reverify.md` is not required.

## Step 9 Verify and Reach a Review Conclusion

The exact-file command `AX_TEST_FILES=test/server/app-context-routes.test.ts pnpm exec vitest run`, executed from `packages/ax-code`, passed 1 file and all 5 tests. The separate `pnpm --dir packages/ax-code run typecheck` command also exited successfully. These checks exercised the metadata route asserted at `packages/ax-code/test/server/app-context-routes.test.ts:23-40`, error propagation at lines 68-96, and template POST at lines 101-117, while static typing checked the schema-to-producer imports at `packages/ax-code/src/server/routes/app-context-templates.ts:2` and `packages/ax-code/src/server/routes/app-context-checks.ts:5`. The unit is review-complete with no Critical finding and one low-severity response-contract follow-up.
