# Nine-step review protocol: cli-cmd-release

## Step 1 Scope and entry points

The reviewed unit `cli-cmd-release` is rooted at `packages/ax-code/src/cli/cmd/release`. It exposes two distinct surfaces from `packages/ax-code/src/cli/cmd/release/check.ts`: a back-compat 2-check API at `packages/ax-code/src/cli/cmd/release/check.ts:43` (`releaseReadinessChecks`, plus `decodeReleasePackageJsonValue` at `packages/ax-code/src/cli/cmd/release/check.ts:83` and `parseReleasePackageJsonText` at `packages/ax-code/src/cli/cmd/release/check.ts:91`) and a full runner at `packages/ax-code/src/cli/cmd/release/check.ts:663` (`runChecks`) driving nine checks enumerated at `packages/ax-code/src/cli/cmd/release/check.ts:643-657`. The yargs command wiring sits at `packages/ax-code/src/cli/cmd/release/check.ts:728` (`ReleaseCheckCommand`) with its parent tree at `packages/ax-code/src/cli/cmd/release/index.ts:12` (`ReleaseCommand`), and the parent is registered in the CLI bootstrap at `packages/ax-code/src/cli/boot.ts:97`. The 17-line `index.ts` only registers the subcommand and `demandCommand()`.

## Step 2 Trust boundaries and abuse cases

This is a same-user local CLI, so the trust boundary is repo state plus outbound network. Network side effects are gated behind `--fetch` at `packages/ax-code/src/cli/cmd/release/check.ts:363-379` (default off, honoring the PRD "validate, don't mutate" contract noted at lines 366-367). All networked git calls are wrapped by `gitWithTimeout` at `packages/ax-code/src/cli/cmd/release/check.ts:292-303` with a 10s cap (`NETWORK_TIMEOUT_MS` at line 290) and `stdin: "ignore"` to avoid pipe inheritance. `isWithdrawnGitHubRelease` at `packages/ax-code/src/cli/cmd/release/check.ts:232-251` shells to `gh release view` with `nothrow` and `timeout`, then defensively casts each tuple element (`[unknown, unknown, unknown]` at line 245) before lowering — no untrusted-string interpolation reaches the shell. User-controlled `--skip` ids are validated against `CHECK_IDS` at `packages/ax-code/src/cli/cmd/release/check.ts:772-775`, throwing on unknown values. The phantom-imports regex at line 445 only captures relative (`./`, `../`) specifiers, so bare-module imports cannot be used to scan outside the repo.

## Step 3 Functional correctness and failure flow

`checkVersion` at `packages/ax-code/src/cli/cmd/release/check.ts:255-285` first rejects non-semver at line 257, then calls `latestTag` at line 267. `latestTag` at `packages/ax-code/src/cli/cmd/release/check.ts:198-230` sorts tags descending (`--sort=-v:refname` at line 203), skips prereleases at line 214, scopes to the same major when `relativeTo` is valid semver (line 210, 215), and falls back to the global latest stable at lines 221-228 for a first release on a new major. The withdrawn-release path at lines 232-251 marks withdrawn only when `isPrerelease === true && text.includes("withdrawn")` (line 247) — the test at `packages/ax-code/test/cli/release-check.test.ts:122-153` exercises this with a stubbed `gh` and confirms the prior tag selection falls back to v5.13.0. `findRepoRoot` at `packages/ax-code/src/cli/cmd/release/check.ts:168-180` reads `Filesystem.callerCwd()` (which prefers `AX_CODE_ORIGINAL_CWD` at `packages/ax-code/src/util/filesystem.ts:182-184`) so the launcher's `--cwd` shim does not lie about the user's shell directory. Skip wiring at `packages/ax-code/src/cli/cmd/release/check.ts:665-674` short-circuits before invoking the check function.

## Step 4 Performance and resource behavior

Every check is wrapped in `timed()` at `packages/ax-code/src/cli/cmd/release/check.ts:151-155`, returning `durationMs` on every result so the renderer can surface cost. Checks execute sequentially in the loop at `packages/ax-code/src/cli/cmd/release/check.ts:665-677` — predictable ordering and cheap fail-fast (version → remote-tag → working-tree first per lines 648-650), but no parallelism; total time is bounded by `checkTypecheck` and `checkTests` which spawn `pnpm` at lines 514 and 545. `checkPhantomImports` at `packages/ax-code/src/cli/cmd/release/check.ts:414-510` is the most file-system-intensive: it reads every tracked `.ts/.tsx` file in `src/` and `script/` via `readFile` at line 449, materializes the comment-stripped content via three regex/split/map passes at lines 457-461, and resolves up to 8 candidate specifiers per import at lines 467-476. No background timers, event listeners, or detached loops are introduced; `Process.run` invocations all set `stdin: "ignore"` (lines 237, 296, 516, 547).

## Step 5 Design and ownership

`check.ts` is cohesive at the responsibility level (all release-readiness concerns in one module) but mixes four concerns in a single 799-line file: the legacy 2-check surface (lines 29-117), the full check registry + nine check implementations (lines 119-639), the human/JSON render (lines 681-724), and the yargs command + handler (lines 726-799). Each check function is a pure `(ctx) => Promise<CheckResult>` with no shared mutable state — the only cross-cutting state is the `CheckContext` constructed once at `packages/ax-code/src/cli/cmd/release/check.ts:777-783`. `cmd.ts` is a 7-line typed pass-through (`packages/ax-code/src/cli/cmd/cmd.ts:5`). Comments throughout cite PRD rationale (e.g. lines 4-5, 62-69, 366-367, 415-420), which is good traceability but signals the file has accreted historical regression patches (v2.21.2 script/ phantom, letter-suffix versions). The render layer at lines 696-724 is small enough that extraction is not yet warranted.

## Step 6 Hygiene and recoverability

Two intentional silent catches exist. `readPackageJSON` at `packages/ax-code/src/cli/cmd/release/check.ts:111-117` swallows read/parse failures and returns `undefined`, which the caller at line 97-100 handles by trying the next candidate. `isWithdrawnGitHubRelease` at `packages/ax-code/src/cli/cmd/release/check.ts:248-250` swallows JSON parse errors and returns `false` (treat as not-withdrawn), the safe default for a release gate. Both are deliberate and explained by surrounding comments. Errors are surfaced via `toErrorMessage` at lines 295, 521, 549 — never stringified opaquely. The handler's outer try/catch at `packages/ax-code/src/cli/cmd/release/check.ts:765-797` writes to `process.stderr` and exits 2 on setup failures, exit 1 on check failures (line 792), exit 0 otherwise — distinct codes that CI can branch on. No `console.*` calls; output is explicit `process.stdout`/`process.stderr` at lines 788, 790, 795. No TODO/FIXME/HACK markers in either file.

## Step 7 Test adequacy

`packages/ax-code/test/cli/release-check.test.ts` (450 lines, 25 tests) covers both surfaces. Legacy decoder/parser tests at lines 69-99, full-runner version validation at lines 105-161, withdrawn-release fallback at lines 122-153, phantom-imports detection plus the v2.21.2 `script/` regression at lines 171-305, commented-import false-positive guard at lines 263-286, dirty-tree failure at lines 227-237, and `CHECK_IDS` sync guard at lines 307-315. The `mkCtx` helper at lines 48-58 skips slow checks by default; targeted tests inject mock `pnpm` and `gh` binaries via `PATH` manipulation (lines 348-379, 397-431) to verify exact argv construction without spawning real builds. Coverage gaps: the yargs `ReleaseCheckCommand.handler` at `packages/ax-code/src/cli/cmd/release/check.ts:764` itself is not invoked — no test exercises `--json` output shape (line 788), `--for-version` override (line 767), `--no-color` rendering (line 790), or the exit-code differentiation at lines 792/796. These are wiring paths, not deep logic, but they are unverified.

## Step 8 Finding reconciliation

The `findings/` directory under `docs/module-quality-audit/modules/cli-cmd-release/` is empty — no `AUDIT-*.md` files exist. The MODULE-AUDIT ledger at `docs/module-quality-audit/modules/cli-cmd-release/MODULE-AUDIT.md:75-77` records `_none accepted_` and the exit checklist at lines 87-91 marks findings-ledger consistency as satisfied. No Critical (or any) severity items are registered, so the conditional `protocol/reverify.md` artifact is not required by the protocol. The Step 7 wiring-coverage observations and the Step 4 sequential-execution note are documented review risks for future hardening, not contradictions of an existing finding — they do not require a new finding file under the current "conservative by default" disposition of the audit program.

## Step 9 Verification and exit assessment

`AX_TEST_FILES=test/cli/release-check.test.ts pnpm exec vitest run` from `packages/ax-code` reported `Test Files 1 passed (1)` / `Tests 25 passed (25)` in 4.86s, exercising the imports at `packages/ax-code/test/cli/release-check.test.ts:6-14` and the runner path at `packages/ax-code/src/cli/cmd/release/check.ts:663`. These tests pin the nine-check registry, the skip wiring at `packages/ax-code/src/cli/cmd/release/check.ts:665-674`, the withdrawn-release prior-tag fallback at lines 232-251, and the phantom-imports detection including the script-directory regression case. The two intentional silent catches (lines 114-116, 248-250) are explained and tested-adjacent. Review exit is acceptable: the unit's release-gate logic is sound, the legacy surface remains back-compat, and the documented risks are coverage-shape and ordering observations rather than blocking defects. No Critical finding requires an independent verifier pass for `cli-cmd-release`.
