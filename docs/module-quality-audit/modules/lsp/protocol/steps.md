# Nine-step review: lsp

Unit slug: `lsp`
Reviewer: codex-sol
Verifier lane: ax-code-glm

## Step 1 Establish Scope and Public Contract

The audited unit is rooted at `packages/ax-code/src/lsp`, as recorded in `docs/module-quality-audit/modules/lsp/MODULE-AUDIT.md:5-17`. Its package-facing entry is deliberately narrow: `packages/ax-code/src/lsp/index.ts:1` re-exports the `LSP` namespace from `index-impl.ts`. That namespace publishes performance snapshots, semantic-envelope types, freshness, normalized diagnostics, and protocol schemas at `packages/ax-code/src/lsp/index-impl.ts:32-43`, while `packages/ax-code/src/lsp/protocol.ts:3-44` defines the Zod-backed Range, Symbol, and DocumentSymbol contracts. The review followed those exports into client lifecycle, selection, caching, diagnostics, point queries, workspace symbols, and prewarming.

## Step 2 Inspect Trust Boundaries and Destructive Operations

The important boundary is a configured language-server process rather than stored credentials. `packages/ax-code/src/lsp/launch.ts:23-35` builds an argv array, requests piped stdio, and defaults to a sanitized environment; custom server configuration can deliberately add command arguments and environment values at `packages/ax-code/src/lsp/server-config.ts:62-109`. JSON-RPC is attached directly to child stdout/stdin at `packages/ax-code/src/lsp/client.ts:211-214`, and initialization is capped at 45 seconds with errors wrapped as `LSPInitializeError` at `client.ts:288-332`. Temporary JDTLS cleanup limits discovery to entries with the owned prefix before recursive removal at `packages/ax-code/src/lsp/jdtls-data-dir.ts:18-29`. No path from server output to shell parsing was found; the main residual trust assumption is that enabled project configuration is authorized to launch its declared executable.

## Step 3 Trace Correctness Through Lifecycle and Requests

Client creation handles spawn absence, spawn exceptions, initialization failure, disposal races, duplicate registration, and immediate process death before adding a client at `packages/ax-code/src/lsp/index-impl.ts:343-441`. Concurrent requests for the same root/server reuse one promise, while distinct pending starts are awaited together at `index-impl.ts:449-529`. Per-file open and close operations serialize through a path-specific lock at `packages/ax-code/src/lsp/client.ts:348-357`; close clears versions, content, and diagnostics even when the server is gone at `client.ts:448-480`; and reopen increments the version, selects incremental or full sync, and updates the local snapshot only after notification succeeds at `client.ts:535-605`. Semantic fan-out always releases an acquired server budget in `packages/ax-code/src/lsp/envelope-runner.ts:88-129`. These paths support orderly recovery without registering a half-initialized client.

## Step 4 Evaluate Concurrency, Memory, and Latency Bounds

The unit places explicit ceilings on expensive state: diagnostics and last-content maps are capped at 1,000 entries and incremental diffs at 1 MB/256 hunks in `packages/ax-code/src/lsp/client.ts:30-47` and `client.ts:236-249,372-386`. Workspace probing stops at 10,000 files, root caching at 2,000 keys, and each server at eight connected roots in `packages/ax-code/src/lsp/index-impl.ts:50-75`; oldest root entries are evicted at `index-impl.ts:328-339`. Broken servers use capped exponential backoff plus TTL/size pruning at `packages/ax-code/src/lsp/broken-server.ts:19-58`. Cache rows expire after 24 hours and pruning is probabilistic at `packages/ax-code/src/lsp/cache.ts:30-39,152-159`, while recent timing samples use a 1,024-entry ring at `packages/ax-code/src/lsp/perf.ts:5-17,29-43`. Independent prewarm starts are collected in parallel at `index-impl.ts:665-687`, and the home-directory guard avoids an eager broad walk at `index-impl.ts:697-720`.

## Step 5 Review Ownership and API Layering

`index-impl.ts` owns shared instance state and orchestration, then delegates focused behavior to scheduler, selection, diagnostics, point, document-symbol, references, workspace-symbol, cache, and notification modules through the imports at `packages/ax-code/src/lsp/index-impl.ts:15-30`. For example, `packages/ax-code/src/lsp/document-symbol.ts:25-72` owns URI conversion, cache probing, RPC parameters, reduction, and the data-only compatibility wrapper; `packages/ax-code/src/lsp/point.ts:76-132` centralizes point-envelope construction; and `packages/ax-code/src/lsp/selection.ts:67-126` owns method-aware filtering and stable priority ordering. Backward-compatible array-returning functions remain alongside provenance-rich envelopes by explicit policy at `index-impl.ts:831-860`. Although the 966-line facade is large, state ownership is singular and the extracted helpers prevent RPC semantics from being duplicated across public methods.

## Step 6 Examine Failure Semantics and Code Hygiene

Suppressed failures are mostly narrow and documented: cache hit-count flushes cannot fail the request at `packages/ax-code/src/lsp/cache.ts:47-56`, exit-time child cleanup is best effort at `packages/ax-code/src/lsp/launch.ts:12-21`, and dead-server close notifications yield to local cleanup at `packages/ax-code/src/lsp/client.ts:448-480`. Oxlint capability probing stops a failed process, logs the error, deletes the pending cache entry, and permits retry at `packages/ax-code/src/lsp/oxlint.ts:21-44`. One non-Critical semantic concern remains: per-item call-hierarchy failures are converted to empty arrays at `packages/ax-code/src/lsp/point.ts:68-73`, allowing the outer envelope to regard that client as successful. A second consistency concern is that missing diagnostic severity normalizes to `info` at `packages/ax-code/src/lsp/diagnostics.ts:47-55` but `Diagnostic.pretty` defaults the same absence to `ERROR` at `diagnostics.ts:57-69`. Neither is a process-safety or data-exposure issue, but both merit follow-up.

## Step 7 Assess Behavioral Test Coverage

Runtime coverage is substantial. Incremental replacements, inserts, deletes, pathological hunks, size fallback, and appends are exercised at `packages/ax-code/test/lsp/incremental.test.ts:15-106`; same-file update serialization and version behavior are exercised at `packages/ax-code/test/lsp/client.test.ts:393-440`; cache keys and full-only writes are checked at `packages/ax-code/test/lsp/cache-probe.test.ts:74-139` and `packages/ax-code/test/lsp/cache.test.ts:24-61`; and diagnostic normalization/de-duplication/stable ordering appears at `packages/ax-code/test/lsp/diagnostics-aggregated.test.ts:80-119,170-218`. Prewarm de-duplication and parallel startup are covered at `packages/ax-code/test/lsp/prewarm.test.ts:54-147`. The call-hierarchy tests cover successful multi-item aggregation and the no-item case at `packages/ax-code/test/lsp/point.test.ts:46-79`, but not a rejected subrequest and its envelope completeness. Some lifecycle checks are source assertions rather than executed failure scenarios at `packages/ax-code/test/lsp/index.test.ts:5-24`, and the Windows launch case returns early on this macOS lane at `packages/ax-code/test/lsp/launch.test.ts:7-20`.

## Step 8 Reconcile the Register and Severity

The current register contains no accepted entry at `docs/module-quality-audit/modules/lsp/MODULE-AUDIT.md:108-113`, and the unit has no files under `docs/module-quality-audit/modules/lsp/findings/`. This pass likewise found no Critical defect, so a secondary Critical confirmation file is not required. The call-hierarchy completeness ambiguity and missing-severity display inconsistency from Step 6 are non-Critical follow-ups because they can misdescribe or omit diagnostic/navigation data but do not create code execution, credential disclosure, irreversible deletion, or unbounded child growth. The audit header currently shows the lanes reversed at `MODULE-AUDIT.md:12-13`; these run artifacts follow the explicit assignment for this review—codex-sol reviewer and ax-code-glm verifier—without modifying the user-owned audit file.

## Step 9 Run Verification and Decide Readiness

The exact `AX_TEST_FILES` run covering every file under `packages/ax-code/test/lsp/` completed with 26 files and 232 tests passing; among the executed regressions were dead-process liveness at `packages/ax-code/test/lsp/client.test.ts:523-548`, envelope fallback shape at `packages/ax-code/test/lsp/envelope-coverage.test.ts:38-164`, and JDTLS/Oxlint guardrails at `packages/ax-code/test/lsp/server-defs.test.ts:6-57`. `pnpm --dir packages/ax-code run typecheck` also completed successfully, checking the public namespace re-export at `packages/ax-code/src/lsp/index.ts:1` and the typed envelope/schema surfaces. With focused tests and static typing green, no Critical item, and the two non-blocking concerns explicitly recorded, the codex-sol review of `lsp` is ready for ax-code-glm verifier handoff.
