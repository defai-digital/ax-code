## Step 1 Scope and public surface

The `cli-cmd-index-graph` unit resolves to `packages/ax-code/src/cli/cmd/index-graph.ts` according to `docs/module-quality-audit/modules/cli-cmd-index-graph/MODULE-AUDIT.md:5-17`. Its contract includes eight exported helpers at `packages/ax-code/src/cli/cmd/index-graph.ts:32-279` and the yargs `IndexCommand` at `packages/ax-code/src/cli/cmd/index-graph.ts:304-731`. The root CLI imports and registers that command at `packages/ax-code/src/cli/boot.ts:28` and `packages/ax-code/src/cli/boot.ts:90`, while `buildIndexReport`, probing, grouping, filtering, and concurrency validation are also reused by the performance command at `packages/ax-code/src/cli/cmd/debug/perf.ts:295-360` and `packages/ax-code/src/cli/cmd/debug/perf.ts:413-435`.

## Step 2 Inputs, trust boundaries, and effects

Caller-controlled numeric and boolean options enter through the builder at `packages/ax-code/src/cli/cmd/index-graph.ts:307-337`; positive-integer validation is repeated in the handler at `packages/ax-code/src/cli/cmd/index-graph.ts:339-346`. The process working directory becomes the `Instance` root, then ripgrep-relative names are joined beneath it at `packages/ax-code/src/cli/cmd/index-graph.ts:340-369`. The important effect boundaries are LSP process/RPC activity during prewarm and probe at `packages/ax-code/src/cli/cmd/index-graph.ts:432-456`, graph mutation through `CodeIntelligence.indexFiles` at `packages/ax-code/src/cli/cmd/index-graph.ts:555-585`, and terminal or JSON output at `packages/ax-code/src/cli/cmd/index-graph.ts:347-359` and `packages/ax-code/src/cli/cmd/index-graph.ts:624-648`. Ripgrep is spawned with an argument array and a sanitized environment at `packages/ax-code/src/file/ripgrep.ts:345-373`, so project filenames are not interpolated into a shell command.

## Step 3 Indexing and reconciliation correctness

Extension filtering excludes unmapped and plaintext languages at `packages/ax-code/src/cli/cmd/index-graph.ts:32-36`, and an empty discovery pass deliberately continues when stored graph files exist so orphan rows can be removed at `packages/ax-code/src/cli/cmd/index-graph.ts:373-415`. The command correctly disables pruning for a truncated `--limit` run at `packages/ax-code/src/cli/cmd/index-graph.ts:575-585`, and the builder serializes the batch with a disposable cross-process lock at `packages/ax-code/src/code-intelligence/builder-impl.ts:1164-1190`. A Medium-severity reconciliation defect remains: the command passes `Instance.directory` as a bare prefix at `packages/ax-code/src/cli/cmd/index-graph.ts:583-584`, while both the TypeScript query and native store select `scopePrefix + "%"` at `packages/ax-code/src/code-intelligence/query.ts:350-381` and `crates/ax-code-index/src/file.rs:124-151`. Thus `/repo/proj1` also scopes `/repo/proj10`; a deleted-looking row in the latter can be purged even though it belongs to a sibling root sharing the project id. Existing coverage uses distinct `proj1` and `proj2` names at `packages/ax-code/test/code-intelligence/query.test.ts:609-635` and misses this boundary collision.

## Step 4 Failure, concurrency, and cost behavior

Probe failures are converted into a zero-open readiness result at `packages/ax-code/src/cli/cmd/index-graph.ts:257-275`, and prewarm failure degrades to zero counters before the explicit probe can diagnose each language at `packages/ax-code/src/cli/cmd/index-graph.ts:433-474`. The indexing catch clears the heartbeat and marks `AutoIndex` failed before rethrowing at `packages/ax-code/src/cli/cmd/index-graph.ts:586-603`; however, its friendly timeout branch checks `CodeGraphBuilder.LockHeldError` at lines 594-600 even though `lock: "acquire"` is configured at lines 558-567. `IndexLock.acquire` throws a plain `Error` on timeout at `packages/ax-code/src/code-intelligence/lockfile.ts:182-199`, whereas `LockHeldError` is emitted only by the builder's `"try"` path at `packages/ax-code/src/code-intelligence/builder-impl.ts:1181-1184`, making the special message unreachable for this command (Low severity). Worker creation itself is bounded by both concurrency and file count at `packages/ax-code/src/code-intelligence/builder-impl.ts:1257-1283`.

## Step 5 Performance and resource review

The five-second heartbeat uses live `COUNT(*)` calls at `packages/ax-code/src/cli/cmd/index-graph.ts:539-553`; those queries aggregate in SQL without materializing IDs at `packages/ax-code/src/code-intelligence/query.ts:147-159` and `packages/ax-code/src/code-intelligence/query.ts:255-266`. LSP probes intentionally run per-language representatives in parallel at `packages/ax-code/src/cli/cmd/index-graph.ts:248-265`, and the test observes two simultaneous probes at `packages/ax-code/test/cli/index-graph.test.ts:206-228`. A Low-severity cost defect affects benchmarking: the command stops its consumer after reaching `limit` at `packages/ax-code/src/cli/cmd/index-graph.ts:365-371` but does not pass that value to `Ripgrep.files`. The walker documents that native enumeration collects its complete result before yielding unless `input.limit` is supplied at `packages/ax-code/src/file/ripgrep.ts:292-304`, and the native call only receives that input-derived limit at `packages/ax-code/src/file/ripgrep.ts:313-339`; `--limit` therefore may still pay full-tree native traversal and allocation cost.

## Step 6 Dependency ownership and maintainability

The command mostly stays at an orchestration boundary: `Instance.provide` owns project context at `packages/ax-code/src/project/instance.ts:196-213`, `CodeIntelligence.indexFiles` delegates mutation to the graph builder at `packages/ax-code/src/code-intelligence/index.ts:308-318`, and `AutoIndex.setState`/`reportProgress` expose progress rather than duplicating sidebar state at `packages/ax-code/src/cli/cmd/index-graph.ts:512-524` and `packages/ax-code/src/cli/cmd/index-graph.ts:571-574`. Report construction and phase formatting are pure helpers at `packages/ax-code/src/cli/cmd/index-graph.ts:123-190`, which permits reuse without reimplementing the JSON schema. The manually maintained language-install table at `packages/ax-code/src/cli/cmd/index-graph.ts:281-302` is localized but can drift from the central language mapping used at lines 32-50. The most consequential ownership mismatch is the command's assumption that a raw directory string is a containment boundary even though prefix semantics live in `CodeGraphQuery.pruneOrphanFiles`.

## Step 7 Test adequacy and execution

The focused CLI suite covers language grouping, invalid numeric options, phase percentages, report shape, readiness classification, and parallel probing at `packages/ax-code/test/cli/index-graph.test.ts:30-228`. Graph-highlight rendering is exercised against seeded nodes and edges at `packages/ax-code/test/code-intelligence/graph-highlights.test.ts:60-104`, and `buildIndexReport` has another consumer-level exercise at `packages/ax-code/test/cli/debug-perf.test.ts:16-205`. Running `AX_TEST_FILES=test/cli/index-graph.test.ts,test/code-intelligence/graph-highlights.test.ts pnpm exec vitest run` from `packages/ax-code` passed both files and all nine tests. Missing coverage includes `isIndexableFile`, command-level JSON and human flows, the empty-tree prune path, heartbeat cleanup, acquisition timeout diagnostics, forwarding `--limit` into discovery, and the `proj1`/`proj10` scope boundary.

## Step 8 Findings and severity disposition

No finding files exist for this unit, and the scaffold's register reports no accepted entries at `docs/module-quality-audit/modules/cli-cmd-index-graph/MODULE-AUDIT.md:68-72`. Independent review nevertheless identifies three non-Critical issues: Medium severity for raw-prefix orphan pruning evidenced by `packages/ax-code/src/cli/cmd/index-graph.ts:575-585` and `packages/ax-code/src/code-intelligence/query.ts:350-381`; Low severity for the acquire-timeout error-class mismatch evidenced by `packages/ax-code/src/cli/cmd/index-graph.ts:586-600` and `packages/ax-code/src/code-intelligence/lockfile.ts:182-199`; and Low severity for failing to forward the discovery limit evidenced by `packages/ax-code/src/cli/cmd/index-graph.ts:365-371` and `packages/ax-code/src/file/ripgrep.ts:292-339`. They are recorded here because the requested output is limited to protocol artifacts, not source changes or new finding records.

## Step 9 Verification and exit assessment

The targeted Vitest invocation passed 2 files and 9 tests, and `pnpm --dir packages/ax-code run typecheck` completed successfully with `tsgo --noEmit`. The original audit still labels the dual-agent protocol pending at `docs/module-quality-audit/modules/cli-cmd-index-graph/MODULE-AUDIT.md:74-86`; this run supplies the primary review evidence without claiming the three issues in Step 8 are resolved. None is Critical, so the conditional `protocol/reverify.md` artifact is not triggered. Primary reviewer `codex-sol` has completed all nine steps, with the independent `ax-code-glm` lane remaining the verifier named at `docs/module-quality-audit/modules/cli-cmd-index-graph/MODULE-AUDIT.md:12-16`.
