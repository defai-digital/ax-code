# Review protocol: server-routes-app-context

## Step 1 Scope and public surface

The `server-routes-app-context` unit is the single lazy router exported at `packages/ax-code/src/server/routes/app-context.ts:18-180`. Its four operations are metadata retrieval at lines 20-83, template creation at lines 84-122, memory warmup at lines 123-158, and memory deletion at lines 159-180. `packages/ax-code/src/server/server.ts:299-327` mounts that router under `/context`, so those declarations become `GET /context`, `POST /context/template`, `POST /context/memory/warmup`, and `DELETE /context/memory`. I followed the schema, template, check-discovery, instruction, filesystem, instance, and memory dependencies used by those handlers.

## Step 2 Trust and mutation boundaries

Requests reach the unit after runtime/password authentication, state-changing browser-origin checks, rate limiting, directory validation, and `Instance.provide` at `packages/ax-code/src/server/server.ts:166-197,258-278`. `packages/ax-code/src/server/request-directory.ts:27-69` rejects relative, missing, dangerous-root, and sensitive-home directory inputs and realpaths the selected root. GET returns absolute project and instruction paths from `packages/ax-code/src/server/routes/app-context.ts:49-64,73-81`; the three mutating operations can create a template, replace `.ax-code/memory.json`, or unlink that memory file at lines 113-115, 142-146, and 176-178. The template key is closed by `packages/ax-code/src/server/routes/app-context-schema.ts:18-25,45-47`, preventing a request body from directly naming an arbitrary path.

## Step 3 Control-flow correctness

Each handler consistently translates the legacy `/` worktree sentinel to `Instance.directory` at `packages/ax-code/src/server/routes/app-context.ts:38,103,141,177`. GET resolves the two conventional filenames, sorts the effective instruction paths, awaits template existence, and combines checks and memory at lines 44-81. Warmup generates before saving and reports fields from the same generated object at lines 142-156; clear returns the store's deleted/not-found boolean at lines 176-178. There is one conditional-key defect: `dir-rules` is accepted by the request schema (`packages/ax-code/src/server/routes/app-context-schema.ts:18-20`), but `contextTemplates` omits that item when directory equals root (`packages/ax-code/src/server/routes/app-context-templates.ts:40-48`). The route then throws `NamedError.Unknown` at `packages/ax-code/src/server/routes/app-context.ts:105-110`, which the generic named-error path converts to HTTP 500 at `packages/ax-code/src/server/error.ts:164-186` even though the request passed validation.

## Step 4 API contracts and failure semantics

Runtime JSON validation is installed before template lookup at `packages/ax-code/src/server/routes/app-context.ts:101-106`, and validation failures become a shared 400 at `packages/ax-code/src/server/validation.ts:4-13`. The declared success shapes at `packages/ax-code/src/server/routes/app-context-schema.ts:3-47` agree with the required fields emitted by the handlers. Two contract gaps remain. First, memory metadata adds `stale` at `packages/ax-code/src/memory/injector.ts:204-229`, while `AppContextMemory` and the generated GET schema omit it at `packages/ax-code/src/server/routes/app-context-schema.ts:10-16` and `packages/sdk/openapi.json:33566-33597`. Second, each route documents only a 200 response at `packages/ax-code/src/server/routes/app-context.ts:26-35,90-100,129-139,165-175`, despite real 400 and 500 paths. Metadata intentionally logs and degrades an unreadable/corrupt memory file to `null` at `packages/ax-code/src/memory/injector.ts:212-216`; warmup instead preserves corrupt data by letting the load failure escape at `packages/ax-code/src/memory/generator.ts:259-264`.

## Step 5 Filesystem safety and recovery

Atomic temp-file rename protects template and memory writes from partial contents at `packages/ax-code/src/util/filesystem.ts:83-115`, and `packages/ax-code/src/memory/store.ts:225-239` distinguishes absent memory from other unlink failures. However, the root check does not protect nested path components from symlinks. Template destinations include `root/docs/*.md` at `packages/ax-code/src/server/routes/app-context-templates.ts:18-36`, while project memory is fixed at `root/.ax-code/memory.json` by `packages/ax-code/src/memory/store.ts:15-16`. Neither the route nor `Filesystem.write` canonicalizes the final parent and checks containment before rename (`packages/ax-code/src/server/routes/app-context.ts:113-115,142-146`; `packages/ax-code/src/util/filesystem.ts:83-100`), and clear directly unlinks the derived path (`packages/ax-code/src/memory/store.ts:225-239`). A project-controlled `docs` or `.ax-code` directory symlink can therefore create, overwrite, or delete a fixed-name file outside the validated project. This is a High containment observation, limited by fixed basenames and the authenticated/local server boundary but still requiring remediation or explicit acceptance.

## Step 6 Resource and concurrency behavior

GET parallelizes its two conventional file probes and fixed-size template checks at `packages/ax-code/src/server/routes/app-context.ts:45-56,65-70`. Check discovery stops after four unique commands at `packages/ax-code/src/server/routes/app-context-checks.ts:81-99,147-168`, and memory loads use the bounded mtime/size cache described at `packages/ax-code/src/memory/store.ts:86-103`. Warmup is an explicit POST with depth 3 and a 4,000-token budget at `packages/ax-code/src/server/routes/app-context.ts:140-145`; its four scanners run with `Promise.allSettled` at `packages/ax-code/src/memory/generator.ts:267-278`, and token allocation is capped at lines 289-312. Concurrent warmups are last-writer-wins, but atomic rename prevents partial JSON. No request-controlled unbounded response collection, timer, listener, or polling loop is introduced by this route.

## Step 7 Design and code hygiene

The route is appropriately an orchestration layer: HTTP metadata and composition live in `packages/ax-code/src/server/routes/app-context.ts:18-180`, response types in `app-context-schema.ts:3-51`, deterministic template definitions in `app-context-templates.ts:8-117`, and check inference in `app-context-checks.ts:131-227`. Instance ownership remains in `packages/ax-code/src/project/instance.ts:196-248`, while persistence remains in `packages/ax-code/src/memory/store.ts:182-240`. All candidate imports are used, and the candidate has no catch block, TODO/FIXME, mutable module state, or unreachable route. The four identical root-selection expressions at `packages/ax-code/src/server/routes/app-context.ts:38,103,141,177` are a small duplication; centralizing them would also provide one place to enforce canonical descendant checks.

## Step 8 Tests and findings decision

The focused suite covers GET metadata and inferred commands at `packages/ax-code/test/server/app-context-routes.test.ts:12-41`, non-string scripts at lines 43-66, propagated EACCES failures at lines 68-96, and one successful root template write at lines 98-118. It does not exercise memory warmup/clear, the root-level `dir-rules` request, nested directory templates, existing-file preservation, corrupt memory metadata, or symlinked template/memory parents. The existing register says no accepted item at `docs/module-quality-audit/modules/server-routes-app-context/MODULE-AUDIT.md:60-64`, and no file was present under this unit's `findings/` directory. This pass records the High symlink-containment issue from Step 5 plus Low contract/test gaps from Steps 3, 4, and 8. No Critical item exists, so the Critical-only `reverify.md` artifact is not required.

## Step 9 Verification and exit conclusion

From `packages/ax-code`, `AX_TEST_FILES=test/server/app-context-routes.test.ts pnpm exec vitest run` passed one file and all five tests defined at `packages/ax-code/test/server/app-context-routes.test.ts:12-118`. From the repository root, `pnpm --dir packages/ax-code run typecheck` passed with `tsgo --noEmit`, compiling the route at `packages/ax-code/src/server/routes/app-context.ts:1-181` and its dependencies. All nine primary-review steps are complete for `codex-sol`, with `ax-code-glm` named as the independent verifier. The protocol is complete, but clean module sign-off should remain conditional on fixing or explicitly accepting the High nested-symlink boundary issue.
