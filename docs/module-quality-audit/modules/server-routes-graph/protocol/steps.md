# Nine-step review: server-routes-graph

## Step 1 Bound the HTTP Surface

The `server-routes-graph` unit exports the lazy `GraphRoutes` router at `packages/ax-code/src/server/routes/graph.ts:12-13`. It defines structured topology at `packages/ax-code/src/server/routes/graph.ts:14-38` and a format-selecting graph endpoint at `packages/ax-code/src/server/routes/graph.ts:39-88`; the application mounts the router under `/graph` at `packages/ax-code/src/server/server.ts:313-317`. The module is therefore an HTTP adapter over graph construction and formatting, with no mutation endpoint.

## Step 2 Examine Trust and Project Boundaries

Both handlers validate the branded session parameter with `SESSION_ID_PARAM` at `packages/ax-code/src/server/routes/graph.ts:32` and `packages/ax-code/src/server/routes/graph.ts:60`; that schema is defined at `packages/ax-code/src/server/routes/route-params.ts:17-21`. They then call `parseCurrentProjectSessionID` before loading events at `packages/ax-code/src/server/routes/graph.ts:34-35` and `packages/ax-code/src/server/routes/graph.ts:70-72`. The helper loads the session and rejects an incompatible project with HTTP 409 at `packages/ax-code/src/server/routes/session-lookup.ts:16-32`, using project identity and directory/worktree containment at `packages/ax-code/src/session/index.ts:486-493`. Server authentication, origin checks, rate limiting, and project-instance selection precede the mount at `packages/ax-code/src/server/server.ts:155-207` and `packages/ax-code/src/server/server.ts:258-277`. JSON output contains graph labels and metadata rather than replay event bodies, as shown by the graph schema at `packages/ax-code/src/graph/index.ts:24-90`.

## Step 3 Trace Validation and Response Selection

The general endpoint defaults `format` to `json` and limits input to eight literal choices at `packages/ax-code/src/server/routes/graph.ts:61-67`; failed validation becomes the shared 400 envelope through `packages/ax-code/src/server/validation.ts:4-12`. Each accepted text format has an explicit branch at `packages/ax-code/src/server/routes/graph.ts:74-85`, while the fallthrough is the typed JSON envelope at line 86. The dedicated topology endpoint returns the discriminated line objects produced by `topologyLines` at `packages/ax-code/src/server/routes/graph.ts:34-36`, whose response schema is declared at `packages/ax-code/src/graph/format.ts:141-188`. Missing sessions become 404 through `Session.get` at `packages/ax-code/src/session/index.ts:468-473` and the global error normalization at `packages/ax-code/src/server/server.ts:155-165`.

## Step 4 Assess Cost and Resource Bounds

Every successful request rebuilds the graph synchronously at `packages/ax-code/src/server/routes/graph.ts:35` or `packages/ax-code/src/server/routes/graph.ts:72`. `ExecutionGraph.build` loads ordered replay rows and walks them once at `packages/ax-code/src/graph/index.ts:110-149`, but then computes risk with a second session-level read at `packages/ax-code/src/graph/index.ts:308-325` and `packages/ax-code/src/risk/score.ts:395-410`. Each replay read is capped at 10,000 rows at `packages/ax-code/src/replay/query.ts:13-20` and `packages/ax-code/src/replay/query.ts:116-130`, bounding memory while allowing diagnostic truncation. Formatter helpers repeatedly filter edges and search nodes at `packages/ax-code/src/graph/format.ts:22-46` and `packages/ax-code/src/graph/format.ts:67-87`, so large allowed graphs can incur quadratic formatting work. This is a scalability concern, not evidence of a Critical failure for the current bounded input.

## Step 5 Check Responsibility Separation

The route confines itself to HTTP schema, validation, content negotiation, and response construction at `packages/ax-code/src/server/routes/graph.ts:14-87`. Replay interpretation and graph invariants belong to `ExecutionGraph`, including node/edge schemas at `packages/ax-code/src/graph/index.ts:9-91` and event-to-node construction at `packages/ax-code/src/graph/index.ts:149-306`. Presentation remains in `GraphFormat`, with structured topology at `packages/ax-code/src/graph/format.ts:251-307` and the SVG renderer at `packages/ax-code/src/graph/format.ts:478-619`. Project authorization is centralized in `packages/ax-code/src/server/routes/session-lookup.ts:10-32`, rather than duplicated by this router. These boundaries keep route ownership narrow and make graph semantics reusable by non-HTTP consumers.

## Step 6 Review Error Contracts and Maintenance Signals

The route declares 400 and 404 responses at `packages/ax-code/src/server/routes/graph.ts:20-30` and `packages/ax-code/src/server/routes/graph.ts:45-58`, but its shared project guard can emit 409 at `packages/ax-code/src/server/routes/session-lookup.ts:16-21`; the generated contract likewise stops at 400/404 at `packages/sdk/openapi.json:29886-29916` and `packages/sdk/openapi.json:29967-30002`. This is a non-Critical API documentation gap. A second contract discrepancy is that `svggantt` explicitly returns `image/svg+xml` at `packages/ax-code/src/server/routes/graph.ts:77`, whereas the documented 200 media types are only JSON and plain text at `packages/ax-code/src/server/routes/graph.ts:46-55` and `packages/sdk/openapi.json:29968-29981`. The candidate contains no catch block, suppression directive, TODO, FIXME, or mutation path; its lazy wrapper caches successful router initialization and retries failures according to `packages/ax-code/src/util/lazy.ts:7-32`.

## Step 7 Match Tests to Observable Behavior

Executable graph tests cover pending tool calls, step duration/token updates, containment edges, and malformed legacy token/numeric/string fields at `packages/ax-code/test/graph/execution-graph.test.ts:10-171`. The project-identity integration test proves that an explicit cross-project session read receives 409 at `packages/ax-code/test/server/project-identity.test.ts:31-48`, supporting the shared guard but exercising `/session`, not `/graph`. No test read during this review directly requests `/graph/:sessionID`, enumerates all eight format branches, asserts SVG content type, validates the topology response schema, or checks missing/cross-project graph errors. That is meaningful route-level coverage debt, even though the underlying builder has focused regression coverage.

## Step 8 Reconcile Findings and Severity

The audit ledger reports no accepted item at `docs/module-quality-audit/modules/server-routes-graph/MODULE-AUDIT.md:60-64`, and the unit has no finding document to independently re-check. This pass identified two Low contract issues: the omitted 409 response and omitted `image/svg+xml` response media type, evidenced in Step 6. It also records the direct-route test gap and bounded large-graph cost as follow-up quality risks rather than demonstrated production defects. None exposes another project, mutates state, loses data, or meets Critical severity, so the primary `codex-sol` review does not create `protocol/reverify.md`.

## Step 9 Execute Verification and Close

The exact-file command `AX_TEST_FILES=test/graph/execution-graph.test.ts,test/server/project-identity.test.ts pnpm exec vitest run` completed with two files and six tests passing; those cases begin at `packages/ax-code/test/graph/execution-graph.test.ts:10` and `packages/ax-code/test/server/project-identity.test.ts:12`. The package typecheck script is defined as `tsgo --noEmit` at `packages/ax-code/package.json:8-10`, and `pnpm --dir packages/ax-code run typecheck` completed successfully. Source flow, related graph/format ownership, generated OpenAPI, test evidence, and the empty finding register have now received all nine primary-review passes for `server-routes-graph`.
