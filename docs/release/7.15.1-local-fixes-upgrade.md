# AX Code 7.15.1 本地修复迁移

日期：2026-09-09（America/Halifax）。最新稳定发布通过 GitHub release API 和远程版本标签核对：
[官方 7.15.1](https://github.com/defai-digital/ax-code/releases/tag/v7.15.1)，发布时间 2026-09-09T22:23:08Z，源码提交 `c29ca5998fa754eed648d5981a04c368e31333e8`。

## 升级前记忆

先将 7.15.0 的全部本地修复记录在工作区 `memory/ax-code/7.15.0-local-fixes.md`，保存 17 个文件、相对官方版本的完整补丁以及测试/安装证据到 `memory/ax-code/7.15.0-snapshot/`。包括已提交 Redo 修复和后续未提交修复。

原 7.15.0 工作树和主分支均保留。新工作树 `tmp/publish-v7.15.1`，本地分支 `local/v7.15.1-fixes-performance-20260909`，未 commit / push。

## 上游与本地修复处理

| 修复 | 7.15.1 处理方式 |
| --- | --- |
| 部分/完整 Redo 文件增删改恢复、手动文件保留、元数据失败回滚 | 上游已经合入，直接保留官方实现 |
| Git 含方括号等特殊字符的文件名误匹配相邻文件 | 保留 7.15.1 新增 literal-pathspecs 修复及额外测试 |
| 并发 preview/revert/unrevert/cleanup 一致性 | 重新应用本地按工作树串行化 |
| RPC stdout EOF 后立即终止等待 | 重新应用本地修复 |
| 撤销点不在最近 100 条历史内而操作阻塞 | 重新应用自动分页、窗口保留、过期请求保护和重试 |
| /restore-all 与普通逐轮 /redo | 重新应用完整恢复入口及确认流程 |
| Solid 初始化取消刚启动的历史加载 | 重新应用单一 effect 协调 |
| 输入框抢占确认 Enter | 重新应用弹窗按键归属与输入框提交保护 |
| 控制台中文/边框及 Snapshot 批量路径检查性能 | 保留已有官方实现 |

依赖 lockfile 与 7.15.0 相同，复用已验证的 Node 26.8.1 和依赖构建新版完整程序。13 个修复源码/测试文件与 memory 快照核对一致；上游 Redo 测试额外追加了新用例，原用例完整保留。Snapshot 源文件与官方 7.15.1 完全一致。

## 验证结果

- 主要回归 59 项通过，Windows 控制台 12 项通过：合计 **71 项通过、0 失败**。
- TypeScript、Prettier、git diff --check 通过。
- 完整构建包使用独立 Git / SQLite 验证连续撤销、部分/完整恢复、增删改文件、中文空格路径、CRLF、手动文件保留、消息清理。
- 350 条实际存储消息通过四页找回撤销点并正确恢复。
- 实际终端重新进入会话自动显示 301 messages reverted；/redo 后变为 300 并正确恢复文件；/restore-all 的 Enter 确认成功、撤销提示消失；Ctrl+C 正常退出。
- 统一迁移文件换行格式后的构建 SHA-256 与已验证构建完全一致。

候选包 SHA-256：`8984756f070852117e245dfa73722fd48c8ceed7fd569de1c416383de96e9d4c`。

证据目录：工作区 `tmp/upgrade-7.15.1/`，包含 `tests.json`、`console-tests.json`、`history-bundle-retest.json`、`ui-validation.json`、`parity.json` 和安装完成后的 `installed.json`。

测试没有对用户真实会话执行 Undo / Restore。历史越早，需要的分页和内存越多。既有跨进程锁限制以及用户明确暂缓的 RPC/SSE 大响应限制未在此次升级中扩大修复范围。
