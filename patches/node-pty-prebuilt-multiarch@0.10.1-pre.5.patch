diff --git a/scripts/install.js b/scripts/install.js
index d88b7f108b4a1a3429bf50d72884f9a5a84158f4..f919d490b0afee45ceac758a3c14aa1d0cf0eb6c 100644
--- a/scripts/install.js
+++ b/scripts/install.js
@@ -8,9 +8,37 @@ const gypArgs = ['rebuild'];
 if (process.env.NODE_PTY_DEBUG) {
   gypArgs.push('--debug');
 }
-const gypProcess = spawn(os.platform() === 'win32' ? 'node-gyp.cmd' : 'node-gyp', gypArgs, {
+const isWindows = os.platform() === 'win32';
+const env = { ...process.env };
+if (isWindows) {
+  for (const name of [
+    'CFLAGS',
+    'CXXFLAGS',
+    'CPPFLAGS',
+    'LDFLAGS',
+    'CL',
+    '_CL_',
+    'LINK',
+    '_LINK_',
+    'npm_config_cflags',
+    'npm_config_cxxflags',
+    'npm_config_cppflags',
+    'npm_config_ldflags'
+  ]) {
+    delete env[name];
+  }
+  env.GYP_DEFINES = [
+    env.GYP_DEFINES,
+    'enable_lto=false',
+    'enable_thin_lto=false',
+    'lto_jobs='
+  ].filter(Boolean).join(' ');
+}
+const gypProcess = spawn(isWindows ? 'node-gyp.cmd' : 'node-gyp', gypArgs, {
   cwd: path.join(__dirname, '..'),
-  stdio: 'inherit'
+  stdio: 'inherit',
+  shell: isWindows,
+  env
 });
 
 gypProcess.on('exit', function (code) {
