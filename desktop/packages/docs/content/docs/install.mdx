---
title: Install
description: Install AX Code Desktop on macOS, Windows, or Linux.
---

# Install

Supported Desktop install paths:

- macOS Apple Silicon app through Homebrew or DMG
- Windows x64 / ARM64 app through installer or portable ZIP
- Linux x64 / arm64 app through `.deb` or AppImage (Ubuntu 24.04+ glibc)

Desktop source code lives in the AX Code monorepo under `desktop/`, and Desktop
release artifacts are published from the main
[AX Code GitHub releases page](https://github.com/defai-digital/ax-code/releases).
The CLI formula and Desktop cask share the DefAI Digital Homebrew tap.

## Runtime Requirement

AX Code Desktop needs the local AX Code CLI/runtime for coding sessions. The
root [AX Code README](https://github.com/defai-digital/ax-code#readme) shows
the paired CLI and Desktop install commands. After installation, verify the CLI:

```bash
ax-code --version
```

If `ax-code` is not found, install the AX Code CLI before starting Desktop
coding sessions.

## macOS

Homebrew is the recommended macOS path. Install the CLI runtime and Desktop app together on a new machine:

```bash
brew install defai-digital/tap/ax-code                  # CLI/runtime
brew install --cask defai-digital/tap/ax-code-desktop   # Desktop app
```

If the CLI is already installed, only run the cask command. The Desktop cask token is `ax-code-desktop` —
it intentionally differs from the `ax-code` CLI formula, because Homebrew refuses to link a formula while an
installed cask shares its token (which used to leave the `ax-code` command missing after upgrades). Installs
of the short-lived `ax-code` cask token migrate to `ax-code-desktop` automatically on their next
`brew upgrade`.

You can also download the latest macOS DMG from the [GitHub releases page](https://github.com/defai-digital/ax-code/releases). Open it, drag **AX Code** into **Applications**, and launch it from Applications.

## Windows

Download the latest Windows installer from the [GitHub releases page](https://github.com/defai-digital/ax-code/releases) and run it. For portable use, download the Windows ZIP, extract the full folder, and run `AX Code.exe` from the extracted folder.

## Linux (Ubuntu Desktop 24.04+)

Install the paired CLI first, then download a Desktop package from a `desktop-v*` release:

- amd64: `AX-Code-<version>-linux-amd64.deb` or `AX-Code-<version>-linux-x86_64.AppImage`
- arm64: `AX-Code-<version>-linux-arm64.deb` or `AX-Code-<version>-linux-arm64.AppImage`

```bash
# CLI
curl -fsSL https://raw.githubusercontent.com/defai-digital/ax-code/main/install | bash

# Desktop .deb
sudo apt install ./AX-Code-<version>-linux-amd64.deb

# or portable AppImage
chmod +x AX-Code-<version>-linux-x86_64.AppImage
./AX-Code-<version>-linux-x86_64.AppImage
```

AppImage is the Linux auto-update channel. Musl distros are not supported.

## Platform capabilities

| Capability               | macOS Apple Silicon                      | Windows x64 / ARM64 | Linux x64 / arm64 |
| ------------------------ | ---------------------------------------- | ------------------- | ----------------- |
| Desktop app              | Supported                                | Supported           | Supported         |
| Hosted providers         | Supported                                | Supported           | Supported         |
| AX Engine local provider | Supported on eligible Apple Silicon Macs | Not supported       | Not supported     |

AX Engine is a local MLX/Apple Silicon provider. On Windows and Linux, use hosted providers or an OpenAI-compatible model provider; remote AX Code servers are unavailable.

## Development checkout

Contributor builds run from the monorepo:

```bash
pnpm install
pnpm run desktop:build
# macOS
node desktop/packages/electron/scripts/package.mjs --mac zip --publish=never
# Linux (on Ubuntu)
node desktop/packages/electron/scripts/package.mjs --linux --x64 --publish=never
```
